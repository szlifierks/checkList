<Window x:Class="checkList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="sprawdzacz" Height="450" Width="800"
        Icon="Assets/icon.ico">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Background" Value="#007ACC" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#005A9E" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="Red" />
        </Style>
        <Style TargetType="Border">
            <Setter Property="BorderBrush" Value="#CCCCCC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="5" />
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Czy zainstalowano sterowniki" x:Name="TextBlock1" Grid.Row="0" Grid.Column="0"
                   VerticalAlignment="Center" />
        <Button Content="Attach Picture" Click="AttachPicture1_Click" Grid.Row="0" Grid.Column="1" />
        <Border Grid.Row="0" Grid.Column="2">
            <Image x:Name="Image1" />
        </Border>

        <TextBlock Text="Czy dodano do witryny" x:Name="TextBlock2" Grid.Row="1" Grid.Column="0"
                   VerticalAlignment="Center" />
        <Button Content="Attach Picture" Click="AttachPicture2_Click" Grid.Row="1" Grid.Column="1" />
        <Border Grid.Row="1" Grid.Column="2">
            <Image x:Name="Image2" />
        </Border>

        <TextBlock Text="Czy zaktualizowano zasoby" x:Name="TextBlock3" Grid.Row="2" Grid.Column="0"
                   VerticalAlignment="Center" />
        <Button Content="Attach Picture" Click="AttachPicture3_Click" Grid.Row="2" Grid.Column="1" />
        <Border Grid.Row="2" Grid.Column="2">
            <Image x:Name="Image3" />
        </Border>

        <Button Content="Submit" Click="SubmitClick" x:Name="SubmitButton" IsEnabled="False" Grid.Row="3"
                Grid.Column="0" Grid.ColumnSpan="3" HorizontalAlignment="Center" VerticalAlignment="Bottom"
                Padding="15,10" />
    </Grid>
</Window>